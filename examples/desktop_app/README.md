# Desktop application example

This example serves embedded production assets from owebview's private local
application origin and demonstrates managed secondary windows, native dialogs,
theme queries, console forwarding, and Eio-owned close lifecycle.

Run it locally with:

```sh
dune exec examples/desktop_app/desktop_app.exe
```
